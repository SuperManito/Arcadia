---
title: 创建
---
import APITable from '@site/src/components/APITable';
import { POST } from '@site/src/components/RequestTag';

<POST>/token</POST>

## 请求

<APITable>

| 名称 | 类型 | 必填 | 描述 |
| :-: | :-: | :-: | --- |
| `name` | `string` | 是 | 配置名称 |
| `expire_time` | `number` | 否 | 过期时间（时间戳），不提供为永久 |
| `permissions` | `string` | 否 | 访问权限，默认为空（即不限制访问权限），仅支持 `env \| cron \| file` 三种类型，用逗号拼接 |

</APITable>

## 响应

参考[获取列表响应](./list.mdx#响应)
